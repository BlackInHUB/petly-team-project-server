const newMessage = require('./newMessage');
const get = require('./get');
const remove = require('./remove');
const readed = require('./readed');

module.exports = {
    newMessage,
    get,
    remove,
    readed
};
